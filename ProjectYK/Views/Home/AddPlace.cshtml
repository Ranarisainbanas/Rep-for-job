@model ProjectYK.Models.Place
@{
    ViewBag.Title = "Добавление места проведения мероприятий";
}
<form asp-action="AddPlace" asp-controller="Home">
    <div class="form-group">
        <label for="Name">Название</label>
        <input type="text" asp-for="Name" class="form-control" name="Name" />
    </div>    
    <div class="form-group">
        <input type="submit" value="Добавить" class="btn-light" />
    </div>
</form>